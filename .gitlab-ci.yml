stages:
  - docs


docs-build:

  stage: docs

  tags:
    - docker


  script:

    - echo "start docs"

    - node --version

    - npm --version

    - corepack enable

    - pnpm --version

    - pnpm install --frozen-lockfile

    - pnpm docs:build